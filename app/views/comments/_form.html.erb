<%= form_for [post, Comment.new] do |f| %>
  <%= f.hidden_field :authenticity_token, value: form_authenticity_token %>
  <%= f.text_area :content, rows: 6, cols: 20 %>
  <%= f.submit "Submit" %>
<% end %>